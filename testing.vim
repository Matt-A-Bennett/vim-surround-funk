
Major Bugs: 
a. with gsw on hello in case 3:
a. with gsW on np.hello in case 2:
a. with gsf on hello in case 1:
a. with gsF on hello in case 1:

1. np.mean(np.hello())
2. np.std(np.hello )
3. np.boot(np.hello)
