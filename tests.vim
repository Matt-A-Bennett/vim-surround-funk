" IS
" GOOD

" test0:
" np.out(os.in())
" np.os.in()

" test1:
" np.out(os.in)
" os.in

" test2:
" np.out(os.in())
" np.helloos.in()

" test3:
" np.out(os.in)
" helloos.in

" test4:
" np.out(os.in())
" np.out(os.in())
" np.out(os.out(in()))

" test5:
" np.out(os.in)
" np.out(os.in)
" out(np.out(os.in))

" test6:
" np.out(os.in)
" np.out(os.in)
" np.out(np.out(os.in))

